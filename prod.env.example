NODE_ENV=production
APP_PORT=3000
# Публичный URL сайта (origin, без слеша в конце) — Open Graph, canonical. Обязательно задать в проде.
PUBLIC_SITE_URL=https://example.com
BACKEND_URL=http://localhost:4000
# /api/<path> -> {BACKEND_URL}/{API_REWRITE}<path>
API_REWRITE=api/v1/
