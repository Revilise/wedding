NODE_ENV=development
APP_PORT=3000
# Публичный URL сайта (origin, без слеша в конце) — Open Graph, canonical. Пример: https://wedding.example.com
PUBLIC_SITE_URL=http://localhost:3000
BACKEND_URL=http://localhost:4000
# /api/<path> -> {BACKEND_URL}/{API_REWRITE}<path>
API_REWRITE=v1/
