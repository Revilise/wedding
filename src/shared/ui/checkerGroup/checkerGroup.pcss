.checkerGroup {
  --checkGroupOptionsGap: 1rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
  
  &__options {
    gap: var(--checkGroupOptionsGap);
    display: flex;
    border: none;
    outline: none;
  }
}
