.checkerGroup {
    --checkGroupOptionsGap: 1rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    user-select: none;

    &__options {
        gap: var(--checkGroupOptionsGap);
        display: flex;
        border: none;
        outline: none;
    }
}
